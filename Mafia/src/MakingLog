05-09
	 한글이 잘 보인다.
	 Game 클래스 생성.
	 	-> Main에서 게임에 접속하기 위한 용도.
	 	-> 게임의 모든 시스템(낮밤, 룰, 투표 등 모두 설정)

05-10
	Server에서 모든 Clients로 메세지 보내는 기능(sendToAll 제대로 작동 안함...)
	ServerSender+Receiver 통합 => ?
	
05-14
	까먹기 전에 메모
	지금은 클라이언트에서 메세지를 입력하면, 서버에서 그 메세지만을 받아서 메세지를 보낸 클라이언트를 포함한 모두에게 메세지를 뿌려준다.
	-> 메세지를 보낸 클라이언트의 key값도 보내서 본인에겐 출력이 안되게 해야함.
	=>OK!
	
	이제 sendToAll을 수정해야지
	=> ServerSender 클래스 대대적 수정. 스트림 빼고, 생성자 지우고, 순전히 sendToAll 함수만을 사용
	-> 사용자가 접속해있지 않을때 메세지 보내면 어떻게 해야하는지
	=> 우선 OK!